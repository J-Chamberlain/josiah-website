# Codex Prompt 06: Search + Tags (Deferred)

This prompt is deferred for v1.

## v1 Decision
- No on-site search feature in v1
- No server-side search API in v1
- Keep tag chips and tag pages only if already implemented with minimal overhead

## v1.1 Target
- Implement static search with Pagefind
- Index only public (non-unlisted) content
- Keep client JS minimal and avoid adding a backend search service
